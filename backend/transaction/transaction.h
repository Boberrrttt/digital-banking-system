#ifndef TRANSACTION_H
#define TRANSACTION_H

void deposit(string accountId, double amount);
void moneyTransfer(string accoundIt, double amount);
void transactionHistory(string transactionId);

#endif
